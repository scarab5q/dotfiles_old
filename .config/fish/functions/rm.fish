# Defined in - @ line 1
function rm --wraps=echo\ \'mate\ use\ trash\ put\ instead,\ if\ you\ really\ want\ to\ use\ /usr/bin/env\ rm\' --description alias\ rm\ echo\ \'mate\ use\ trash\ put\ instead,\ if\ you\ really\ want\ to\ use\ /usr/bin/env\ rm\'
  echo 'mate use trash put instead, if you really want to use /usr/bin/env rm' $argv;
end
